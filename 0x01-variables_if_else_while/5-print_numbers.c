#include <stdio.h>

/**
 * main - Entry point
 *
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	/** printing numbers below 10 */
	for (i = 0 ; i < 10 ; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
